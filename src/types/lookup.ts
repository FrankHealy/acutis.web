export interface Country {
  id: string;
  name: string;
  isoCode?: string;
}

export interface AddictionType {
  id: string;
  name: string;
}

export interface ProbationRequirement {
  id: string;
  description: string;
}

export interface MedicalInsuranceProvider {
  id: string;
  name: string;
}

export interface ReligiousAffiliation {
  id: string;
  name: string;
}

