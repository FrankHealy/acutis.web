export interface Resident {
  id: string;
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  admissionDate: string;
  admissionPhase: string;
  countryId?: string;
  primaryAddictionId?: string;
  createdAt?: string;
}

